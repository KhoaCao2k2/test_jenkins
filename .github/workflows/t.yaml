name: hello world

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run a script
        run: |
          echo "hello world"
      - name: run a script
        run: |
          echo "hello world"